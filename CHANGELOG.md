# Changelog

All notable changes to memory-cli are documented here.
Format: [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Fixed
- **Hygiene batch (task-74c20c2e)** — daemon: test-only guard on tmp-HOME idle floor (`PYTEST_CURRENT_TEST` / `MEMORY_DAEMON_TEST_SHORT_IDLE`); client socket finally-close on all paths (fd leak on autostart retry); model loader `atexit` close silences Llama.__del__ TypeError. Search: remove dead SearchTimeoutError isinstance branch; refuse nested `_SearchDeadline` (shared SIGALRM); 1-in-10 sample latency side-channel on file-backed DBs (`:memory:` always records). R6: `_embed_identity` uses `dimensions` (not dead `dims`) + CHANGELOG wording.

### Added
- **BFS level-batch + confidence-once (R7 / perf-fix-plan #2)** — `spread()` resolves edges.confidence once (meta.schema_version ≥5, no per-node `PRAGMA table_info`); BFS fetches neighbors with one `WHERE source_id IN (...) OR target_id IN (...)` per depth level (O(depth) edge queries, not O(frontier)). Max-activation-wins + hop_distance parity preserved. No id(conn) schema cache. Proven by `tests/search/test_r7_bfs_schema_cache_cte.py` (3/3).
- **Config required on search embed path (R5 / #66 #67 close)** — kill bare `load_config()` fallback in `_run_retrieval_stage`; `light_search(config=None)` raises explicit `ValueError` (not soft exit_code=2, not silent wrong-store model). Central `~/.memory/models/default.gguf` resolution for null/absent `model_path` remains (guards mutation-proven). Heavy path threads `config=` into light_search. Proven by `tests/search/test_r5_model_resolution.py` (3/3).
- **Multi-store embed-once + merge-before-hydrate (R6 / NEW-3)** — layered LOCAL+GLOBAL `handle_search` embeds the query once per embed-config identity (`model_path`,`dimensions`) and reuses the vector across same-config stores; differing embed configs still embed per-store (never cross-model reuse). Per-store pipeline runs with `defer_hydration`; candidates merge/sort/truncate first, then `hydrate_results` runs only on the final page (via orchestrator seam so R4 `track_access=False` holds). `SearchOptions` gains `query_embedding` / `embedding_provided` / `defer_hydration`. Single-store path unchanged. Proven by `tests/search/test_r6_multistore_embed_once.py` (3/3).
- **Daemon introspection + flock single-instance (ADR R6)** — `fcntl.flock(LOCK_EX|LOCK_NB)` on pidfile before model load; losing daemon exits without loading GGUF. Thick `memory embed daemon` status envelope (state,pid,instance_count,rss_kb,uptime_s,model_path,dims,socket,embed_count,lock_held); `meta health` nested `{daemon:{state,pid,instance_count,rss_kb}}`. instance_count lock-derived (not pgrep). Proven by `tests/embedding/test_r6_daemon_introspection.py` (4/4).
- **True read-only search (R4)** — search path does ZERO persistent writes: (1) v010 scopes `trg_neurons_fts_update` to `AFTER UPDATE OF content`; (2) access_count bump opt-in only (`hydrate_results(track_access=False)` default); (3) latency INSERT via side-channel file connection (same-conn for `:memory:` tests); (4) extension probe uses `SELECT vec_version()` / `pragma_module_list` (no CREATE/DROP `_vec_test`/`_fts5_test`). Enables RO connections + concurrent fleet reads. Proven by `tests/search/test_r4_true_read_only_search.py` (6/6).
- **Hard per-query search timeout + self-reap (R3)** — wall-clock ceiling (default 120s, CLI `--timeout <s>`) via SIGALRM around `light_search`; breach raises `SearchTimeoutError` naming the stage reached (`embed`/`bm25`/`vector`/`activation`/…); CLI returns structured `status=error` (exit ≠ 0). Clean teardown (conn still usable). #72 fix-4 wedge class. Proven by `tests/search/test_r3_hard_timeout_self_reap.py` (7 reds green).
- **Resident embedding daemon (R1 / ADR 0001)** — warm GGUF across CLI processes over a unix socket (`~/.memory/run/embedd.sock`). Modules: `embedding_daemon_server.py` (`embed_texts` → `embed_batch`), `embedding_daemon_client.py` (`embed` always batch shape; inproc fallback on any failure, never hang). Config: `embedding.daemon_{embed,connect,idle}_timeout_s` + `daemon_n_threads` (defaults 30/2/600/4); env overrides `MEMORY_EMBED_{TIMEOUT,IDLE_TIMEOUT,N_THREADS}_S` / `MEMORY_EMBED_N_THREADS`. CLI: `memory embed daemon --bg|--stop`; `memory meta health` reports daemon up/down. Search seam routes query embed through the client. INV-1 embed-only (no search/cli/db imports in server). Thread cap applied in `get_model`. Proven by `tests/embedding/test_r1_daemon_acceptance.py` + `tests/search/test_r1_daemon_fallback_integration.py` (AC1 full-CLI wall remains host-bound: Python cold start floor ~90ms + search ~230ms even with warm daemon embed ~16ms).
- **Facet fast-path records `search_latency` (R2)** — `_facet_fast_search` used to return before `_record_latency`, so gate-lookup wins were invisible to health/p50 reporting. Facet lane now calls the shared `_record_latency` helper (retrieval/scoring buckets = 0; wall in `output_ms`) so any future R4 sampling/batch gate applies to both full and facet paths — no open-coded second INSERT. Zero Llama load preserved (guarded by R2 acceptance reds). Fleet lint: gate protocols must use `--type`/`--tag` WITHOUT `--semantic`. #72 status: fix-1 shipped; remaining = daemon (R1) + timeout (R3). Proven by `tests/search/test_r2_facet_fastpath_perf_acceptance.py` (zero-Llama + latency row + <300ms smoke).

### Fixed
- **Facet fast-path returned 0 results on multi-word phrases** — `_rank_facet_candidates` (MEM-FIX-0007) ranked via FTS5 MATCH with implicit AND across all tokens and no fallback: a real multi-word gate phrase (e.g. `search "verify on live path" --type lesson`) returned 0 whenever no single facet row contained every token, even though the facet held dozens of matching rows (droid fleet gate lookups are phrases — every one silently read as "no lessons exist"). Added staged relaxation in `_rank_facet_candidates`: Tier 1 (unchanged quoted-AND MATCH) → Tier 2 (new: OR-joined quoted tokens via new `_build_fts5_query_or` helper, same subset/cap, BM25-ranked) → Tier 3 (new: recency fallback over the facet subset, reusing the existing empty-query branch). Envelope rows carry `match_type` = `facet_or`/`facet_recency` for tier 2/3 so callers can distinguish ranked-match from browse; `facet_fast` stays `True`. Zero model loads on any tier (verified: spy `get_model` never called across all 3 tiers). `_build_fts5_query`/global BM25 semantics, facet resolution, semantic path, and heavy path untouched; no new DB index. Live repro (droid project, 85 type=lesson): `search "verify on live path" --type lesson` 0 → 40 results, 0.2s/95% CPU (was silent-empty). Proven by 8 new tests: `tests/search/test_facet_fastpath_fallback.py` AC-1..6 (AC-1 uses a genuine multi-word phrase against a facet where no row matches all tokens — the exact shape of the shipped bug, not a convenient single/all-match query). Suite 1859 → 1867 passed. (MEM-FIX-0008; commit 0d896af; spec `docs/delegations/MEM-FIX-0008.spec.md`)
- **`--type`/`--tag` search silently dropped + every facet query paid a full embed** — `handle_search` parsed only `--limit`/`--threshold`, so `search --type lesson` ran an unfiltered full pipeline (filter was a no-op) AND still loaded the llama.cpp embed model unconditionally, driving fleet gate-lookup CPU to 100–153% per query and host load 600+ (minion bug #72, droid diagnostics 0048). Added a facet fast-path in `light_search`: when `--type`/`--tag` is set and `--semantic` isn't, resolve candidates via the existing attr/tag indexes (`idx_neuron_attrs_attr_key_id`, `idx_neuron_tags_tag_id`), rank BM25-in-subset (recency for an empty query), and hydrate directly — zero embed/vector/activation, and the filter now actually applies. `--type`/`--tag`/`--semantic` are threaded through `handle_search` into `SearchOptions` (+`ntype`, +`semantic`; envelope +`facet_fast`). Non-facet search is untouched (INV-A). Heavy search opts back in: both `heavy_search_orchestrator` `SearchOptions` builders now set `semantic=True` so tag-scoped heavy sub-queries keep the full pipeline instead of degrading to no-embed BM25-only (INV-B). No new DB indexes. Proven by 11 new tests: `tests/search/test_facet_fast_path.py` AC-1..7 (`get_model.assert_not_called()` on `--type`; `assert_called()` on non-facet and `--semantic`) + `TestHeavyStaysSemanticWithTagFilter` ×2 (heavy tag-scoped still embeds). Suite 1848 → 1859 passed. (MEM-FIX-0007 / backlog #72; commits 647d47e, 9d866cb; gate `docs/delegations/MEM-FIX-0007.gate.md`)
- **Search final-score scale mismatch buried perfect text/vector matches** — `compute_final_scores` ADDED a rank-based `rrf_score` (hard ceiling 2/61 ≈ 0.033) to a 0–1 `tag_affinity_score`, then multiplied by an UNBOUNDED `salience_weight`. Match quality contributed ≤4% of the total; old, frequently-accessed, loosely-tagged neurons permanently outranked verbatim matches (a freshly-added perfect match landed at rank 42). Rewrote stage 8 (`final_score_combine_and_rank.py`): the match-quality signal is now normalized into a DOMINANT 0–1 base (`rrf_score/RRF_MAX` for direct matches, `activation_score` for fan-out, capped affinity for tag-only neighbors); affinity/salience are bounded multipliers centered on 1.0 (salience's unbounded excess is clamped at stage 8 — upstream computation unchanged); temporal applies as its natural 0–1 multiplier. A top-RRF match with weak modifiers now outranks a mid-RRF match with maxed modifiers. Live: target neuron moved rank 42 → 1; regression control moved rank 8 → 1. Orchestrator stage-8 docstring updated to match. (MEM-FIX-0006 / backlog #71)

## [0.4.0] — 2026-06-10

Minor bump (semver): new feature (central model resolution, MEM-FIX-0003) + additive API field (`meta.vector_unavailable_reason`, MEM-FIX-0002) + new `neuron tree` verb. No breaking changes to existing data or search behavior; `model download --local` removal is pre-1.0 cleanup of a flag superseded by central resolution.

### Added
- **`meta.vector_unavailable_reason`** — embedding outages now carry the exception class+message (e.g. `FileNotFoundError: Embedding model not found: ...`) in search meta instead of failing silent behind a bare `vector_unavailable: true`. `null` when vectors are available. (MEM-FIX-0002 / backlog #68)
- **Central model resolution** — explicit config `embedding.model_path` wins if the file exists; otherwise the loader falls back to the central `~/.memory/models/default.gguf`. Fresh local stores no longer silently degrade to BM25-only because their baked per-store model path is absent. (MEM-FIX-0003 / backlog #66)
- **`memory neuron tree <id>`** — recursive multi-hop tree/lineage traversal verb (MEM-FEAT-0001 / backlog #64). Walks descendants (`--direction down`), ancestors (`--direction up`), or both from a root neuron, depth-bounded (`--depth`, default 10), optionally filtered by edge reason (`--type`), cycle-safe via visited set. New module `src/memory_cli/traversal/tree_recursive_lineage.py`; exported from `traversal/__init__.py`; registered in neuron noun handler.

### Changed
- **`memory init` writes `embedding.model_path: null`** instead of baking a per-store `<store>/models/default.gguf` path; `model_path` is no longer a required config field. Resolution defers to the central model at load time. (MEM-FIX-0003)

### Removed
- **`memory model download --local` + auto-symlink to local store** — superseded by central model resolution; one central model serves all stores. (MEM-FIX-0003)

### Fixed
- **`--global` search from a project dir used the LOCAL store's config/model** — the retrieval stage called bare `load_config()` (ancestor walk) instead of the resolved store's config; config is now threaded from `get_layered_connections_with_config` through `light_search`. (MEM-FIX-0001 / backlog #67)
- **`meta stats` / `meta check` crash on `model_path: null`** — `os.path.basename(None)` TypeError on stores initialized post-central-resolution; None-guarded with a `"none"` display sentinel, drift compare only fires when the DB has a model row. (MEM-FIX-0005)
- **Flaky column-existence caches keyed by `id(conn)`** (`edge/edge_list_by_neuron_direction.py`, `search/spreading_activation_bfs_linear_decay.py`). CPython reuses object ids after a connection is GC'd, so the module-level `id(conn)` cache could leak a stale schema flag onto a later same-id connection with a different schema — a non-deterministic cross-test failure (e.g. `test_edge_provenance`). Now query `PRAGMA table_info` each call (a fast local op); correctness over one PRAGMA. (Surfaced while adding `neuron tree`.)

### Known issues
- Test suite has residual pre-existing non-deterministic flakiness (e.g. time-based `test_consolidate_mixed_states`) unrelated to the above — see backlog. Baseline-green should be checked multi-run.
