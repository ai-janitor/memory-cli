You are worker redwiz, a coder in the memory-cli project. Read CLAUDE.md first.

Task 52: Search response time instrumentation and reporting.

Add response time instrumentation to the search pipeline. Track and report p50/p95/p99 search latencies. When response time degrades past a threshold, suggest pruning.

Files to create/modify:
- src/memory_cli/search/light_search_pipeline_orchestrator.py — timing instrumentation
- src/memory_cli/cli/ — memory meta health or memory search stats subcommand

Acceptance criteria:
- Search operations record response time
- memory meta health (or similar) reports search latency statistics
- Warning when p95 exceeds configurable threshold
- Suggests memory neuron prune when latency degrades

Protocol:
1. Write CHECKLIST.md first
2. Read existing search pipeline and meta noun handler code
3. Implement the feature
4. Run uv run pytest when done
5. Commit your work
6. Then run: minion task complete-phase --task-id 52 --agent redwiz
